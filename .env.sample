# Google OAuth credentials (required for Antigravity IDE integration)
ANTIGRAVITY_CLIENT_ID=your-google-oauth-client-id
ANTIGRAVITY_CLIENT_SECRET=your-google-oauth-client-secret

# Tauri dev server host (optional, defaults to localhost)
TAURI_DEV_HOST=localhost
